#Initialize Gradle project:

gradle init --type java-application

# Run Gradle commands:

gradle build   # compiles code + runs tests
gradle run     # runs the application
gradle test    # runs unit tests
